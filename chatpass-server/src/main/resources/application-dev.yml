spring:
  config:
    activate:
      on-profile: dev
      
  datasource:
    url: jdbc:postgresql://localhost:5432/chatpass_dev
    username: chatpass
    password: chatpass123
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.chatpass: DEBUG
    org.hibernate.SQL: DEBUG